# Known Defects (discovered by automation)

- DEFECT: Employee form has no maxlength/minlength or visible validation for unusually long names. (Referenced in `validation.spec.js`)
- DEFECT: Search input is not implemented on the Employee List page. (Referenced in `sortingFiltering.spec.js`)
- DEFECT: Sort controls are not implemented for employee table columns. (Referenced in `sortingFiltering.spec.js`)
- DEFECT: Filter controls are not implemented on the Employee List page. (Referenced in `sortingFiltering.spec.js`)
- DEFECT: Pagination controls are not implemented on the Employee List page. (Referenced in `sortingFiltering.spec.js`)

These entries were recorded while organizing and running tests. Update this file with additional defects discovered during future runs.
